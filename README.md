# vsw-bot
Automatically create wiki pages for new YouTube videos.

## Dev
To install dependencies:

```bash
npm install
```

To run:

```bash
npm run videos
```

To get the cleaned title info:

```bash
npm run titleinfo -- --title "Video title to check"
```

To list all duplicate titles:

```bash
npm run duplicates
```
